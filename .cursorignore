# Cursor-specific ignore patterns
# This file allows you to override .gitignore for Cursor's indexing

# We want Cursor to see .env files for development purposes
# (they remain in .gitignore for git version control)

# Explicitly allow .env files to be indexed and editable by Cursor
!.env
!.env.*
!.env.local
!.env.development
!.env.*.local

