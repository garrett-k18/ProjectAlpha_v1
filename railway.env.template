# Railway Environment Variables Template
# Copy these variables to Railway Dashboard → Service → Variables
# Replace all <placeholder> values with actual credentials

# ============================================================================
# CRITICAL: Database Configuration
# ============================================================================
# Use the UNPOOLED Neon connection string (without -pooler in hostname)
# Railway's health check requires a direct connection to the database
DATABASE_URL=postgresql://neondb_owner:<password>@ep-sweet-unit-afg5w70r.us-west-2.aws.neon.tech/neondb

# ============================================================================
# Django Core Configuration
# ============================================================================
DJANGO_SECRET_KEY=<generate-a-secure-random-secret-key>
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=.railway.app,projectalpha.app
DJANGO_SETTINGS_MODULE=projectalphav1.settings
PYTHONUNBUFFERED=1

# Database Selection (which Neon branch to use)
DJANGO_DB=prod  # Options: dev, newdev, prod

# ============================================================================
# CORS & CSRF Configuration
# ============================================================================
# Add your Railway frontend URL here
DJANGO_CORS_ALLOWED_ORIGINS=https://your-frontend.railway.app,https://projectalpha.app
DJANGO_CSRF_TRUSTED_ORIGINS=https://your-frontend.railway.app,https://projectalpha.app
DJANGO_CORS_ALLOW_ALL_ORIGINS=0
DJANGO_CORS_ALLOW_CREDENTIALS=1

# ============================================================================
# External API Services
# ============================================================================

# Egnyte Document Management
EGNYTE_DOMAIN=projectalpha.egnyte.com
EGNYTE_API_TOKEN=<your-egnyte-api-token>

# AI Services
ANTHROPIC_API_KEY=<your-anthropic-api-key>
GEMINI_API_KEY=<your-gemini-api-key>

# Google Services
GOOGLE_MAPS_API_KEY=<your-google-maps-api-key>

# Microsoft Graph (Outlook Integration)
MICROSOFT_CLIENT_ID=<your-microsoft-client-id>
MICROSOFT_CLIENT_SECRET=<your-microsoft-client-secret>
MICROSOFT_TENANT_ID=<your-microsoft-tenant-id>

# FRED API (Economic Data)
FRED_API_KEY=<your-fred-api-key>

# Geocoding Service
GEOCODIO_API_KEY=<your-geocodio-api-key>

# ============================================================================
# Django Data Upload Limits
# ============================================================================
DJANGO_DATA_UPLOAD_MAX_NUMBER_FIELDS=20000

# ============================================================================
# Cache Configuration (optional)
# ============================================================================
DJANGO_CACHE_KEY_PREFIX=projectalphav1_prod
DJANGO_CACHE_TIMEOUT=300

# ============================================================================
# NOTES
# ============================================================================
# 1. Railway automatically provides $PORT - DO NOT set it manually
# 2. Use Railway's secret management for sensitive values
# 3. For production, generate a strong DJANGO_SECRET_KEY:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 4. Remove -pooler from Neon DATABASE_URL for better connection stability
# 5. Test deployment with: curl https://your-app.railway.app/api/health/

