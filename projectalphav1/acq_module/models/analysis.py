from __future__ import annotations

# Deprecated module: LlDataEnrichment moved to `core.models.enrichment`.
# This module is intentionally left without model definitions.
# Import from `acq_module.models` (re-export) or directly from `core.models.enrichment`.
